[package]
name = "gamelife"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
gamelife-core = { path = "../crates/gamelife-core" }
rusqlite = { version = "0.32", features = ["bundled"] }

[dev-dependencies]
tempfile = "3"
